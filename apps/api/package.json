{
  "name": "@ancient-epics/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787 --ip 127.0.0.1",
    "build": "wrangler deploy --dry-run",
    "lint": "eslint src --max-warnings 0",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "seed:local": "node ./scripts/seed-local.mjs",
    "seed:nuke": "node ./scripts/seed-nuke.mjs",
    "export:local": "node ./scripts/export-local.mjs",
    "smoke": "node ./scripts/smoke-test.mjs"
  },
  "dependencies": {
    "@ancient-epics/shared": "workspace:*",
    "@google/genai": "^1.24.0",
    "hono": "^4.7.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260312.1",
    "better-sqlite3": "^12.6.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.73.0"
  }
}
