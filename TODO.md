1. add a richer chapter editor so split-preview chapters can be manually renamed/edited before session creation
2. persist generated original/translation chapter docs into real draft book/translation records and R2 keys, not just ingestion-session storage
3. tighten the AI prompt/response contract with schema validation and a retry/fix loop when the model returns malformed JSON
